document.addEventListener('DOMContentLoaded', () => {
  const root = document.querySelector('#core-business-solutions');
  if (!root) return;

  // Delegate click navigation has been migrated to standard a tags
});
