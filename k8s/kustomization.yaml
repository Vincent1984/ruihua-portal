apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- configmap.yaml
- mongodb-deployment.yaml
- app-deployment.yaml

images:
- name: ruihua-cms
  newTag: latest

commonLabels:
  project: ruihua-cms
  version: v1.0

namespace: default